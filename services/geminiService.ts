import { GeminiCloudService } from './geminiCloudService';

// Re-export the Cloud Service as GeminiService so the rest of the app uses it automatically
export const GeminiService = GeminiCloudService;
